export interface IMember {
  firstName: string;
  lastName: string;
  region: string;
  dateOfBirth: string;
  hasMedicare: boolean;
  hasMedicaid: boolean;
  isVeteran: boolean;
}

export default IMember;
